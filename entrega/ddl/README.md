# ddl/ — DDL completo do schema

Coloque aqui o resultado do **select único** da Seção 1 do
[GUIA_ENGENHEIRO_DADOS.md](../../GUIA_ENGENHEIRO_DADOS.md):

```
ddl_completo.sql
```

Contém `CREATE TABLE` + `COMMENT ON` (tabela e colunas) de todas as fatos e
dimensões relevantes ao projeto.
