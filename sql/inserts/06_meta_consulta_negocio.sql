-- =============================================================
-- INSERTs — META_CONSULTA_NEGOCIO
-- =============================================================
-- INSERTs para a tabela META_CONSULTA_NEGOCIO.
-- Gerado pela IA a partir das perguntas do INTAKE.
-- O engenheiro deve VALIDAR cada SQL antes de executar.
-- =============================================================

-- IMPORTANTE: Cada SQL deve ser testado e validado pelo engenheiro.
-- Não execute INSERTs sem validar o SQL primeiro.

-- INSERT INTO META_CONSULTA_NEGOCIO (PROJETO, ID_CONSULTA, PERGUNTA_LINGUAGEM_NATURAL, PAINEL_RELACIONADO, OBSERVACOES, SQL_VALIDADO_INTERPRETACAO, OBJETOS_CHAVE) VALUES
-- ('<PROJETO>', '<id>', '<pergunta>', '<painel>', '<observacoes>', q'[<sql>]', '<objetos>');

-- Exemplo de formato:
-- INSERT INTO META_CONSULTA_NEGOCIO (PROJETO, ID_CONSULTA, PERGUNTA_LINGUAGEM_NATURAL, PAINEL_RELACIONADO, OBSERVACOES, SQL_VALIDADO_INTERPRETACAO, OBJETOS_CHAVE) VALUES
-- ('<PROJETO>', 'Q001', 'Qual foi o total de valores?', 'VISAO_GERAL', 'Total geral sem filtros', q'[SELECT SUM(VL_VALOR) FROM FATO_X]', 'FATO_X');

COMMIT;
