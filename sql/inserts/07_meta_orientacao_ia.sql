-- =============================================================
-- INSERTs — META_ORIENTACAO_IA
-- =============================================================
-- INSERTs para a tabela META_ORIENTACAO_IA.
-- Gerado pela IA a partir do template genérico.
-- O engenheiro deve validar antes de executar.
-- =============================================================

-- INSERT INTO META_ORIENTACAO_IA (PROJETO, SECAO, ORDEM, TITULO, CONTEUDO) VALUES
-- ('<PROJETO>', '<secao>', <ordem>, '<titulo>', q'[<conteudo>]');

-- Exemplo de formato:
-- INSERT INTO META_ORIENTACAO_IA (PROJETO, SECAO, ORDEM, TITULO, CONTEUDO) VALUES
-- ('GLOBAL', 'WORKFLOW', 1, 'Passo 1: Entender a pergunta', q'[Leia a pergunta do usuário e identifique o fato e as dimensões envolvidas.]');

COMMIT;
