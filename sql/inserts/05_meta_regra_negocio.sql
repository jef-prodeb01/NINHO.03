-- =============================================================
-- INSERTs — META_REGRA_NEGOCIO
-- =============================================================
-- INSERTs para a tabela META_REGRA_NEGOCIO.
-- Gerado pela IA a partir das regras do INTAKE.
-- O engenheiro deve validar antes de executar.
-- =============================================================

-- INSERT INTO META_REGRA_NEGOCIO (PROJETO, REGRA, SEVERIDADE, STATUS_REGRA, CONTEXTO, OBSERVACAO) VALUES
-- ('<PROJETO>', '<regra>', '<severidade>', '<status>', '<contexto>', '<obs>');

-- Exemplo de formato:
-- INSERT INTO META_REGRA_NEGOCIO (PROJETO, REGRA, SEVERIDADE, STATUS_REGRA, CONTEXTO, OBSERVACAO) VALUES
-- ('<PROJETO>', q'[Não incluir processos cancelados no total anual]', 'CRITICA', 'HOMOLOGADA', 'FATO_X', 'Filtro obrigatório em todas as views');

COMMIT;
