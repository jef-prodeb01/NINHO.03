-- =============================================================
-- INSERTs — META_VALOR_DOMINIO
-- =============================================================
-- INSERTs para a tabela META_VALOR_DOMINIO.
-- Gerado pela IA a partir das amostras dos dados (distincts).
-- O engenheiro deve validar antes de executar.
-- =============================================================

-- INSERT INTO META_VALOR_DOMINIO (PROJETO, NOME_OBJETO, NOME_COLUNA, VALOR, DESCRICAO, IND_ATIVO) VALUES
-- ('<PROJETO>', '<objeto>', '<coluna>', '<valor>', '<descricao>', '<ativo>');

-- Exemplo de formato:
-- INSERT INTO META_VALOR_DOMINIO (PROJETO, NOME_OBJETO, NOME_COLUNA, VALOR, DESCRICAO, IND_ATIVO) VALUES
-- ('<PROJETO>', 'DIM_Y', 'NM_STATUS', 'ATIVO', 'Status ativo', 'S');

COMMIT;
